import RefreshPasswordForm from "@/features/auth/components/RefreshPasswordForm";

export default function RefreshPasswordPage() {
  return (
    <main className="w-full h-full flex justify-center items-center">
      <RefreshPasswordForm />
    </main>
  );
}
